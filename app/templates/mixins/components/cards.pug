include ../media/loader

mixin cardProject(project)
    a(href='/portfolio/' + project.slug)
        .card-portfolio
            .card-media
                img.square(data-src=project.featuredImage.url)
                .utility-loader
                    +loader()
            header.card-header
                .card-subtitle
                    h2
                        if project.categories.length > 0
                            each category, index in project.categories
                                | #{category.key}
                                if (index + 1) < project.categories.length
                                    | , #{' '}
                .card-title
                    .btn-main
                        .btn-title
                            p #{project.title}&nbsp;>
