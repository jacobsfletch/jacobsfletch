extends ../layouts/default

include ../mixins/components/cards

block appBody
    .app-body-part.header
        h1 my blog
    .app-body-part.wall-blog
        .wall-body
            if posts
                each post in posts
                    .wall-body-part
                        +cardPost(post)
            else
                p no posts
